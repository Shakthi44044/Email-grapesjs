/// <reference types="react" />
export interface Props {
    value: boolean;
    border?: boolean;
    placeholder?: string;
    label?: React.ReactNode;
    size?: 'm' | 's';
    onChange(checked: boolean): void;
}
export default function SwitchField({ value, label, border, size, onChange }: Props): import("react/jsx-runtime").JSX.Element;
