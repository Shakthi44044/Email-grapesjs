import { MouseEvent, ReactNode } from 'react';
import { StudioBaseProps } from './StudioBase';
import { ButtonWithTooltipProps } from '../Button/ButtonWithTooltip';
import type { Editor } from 'grapesjs';
export declare enum StudioButtonVariant {
    primary = "primary",
    outline = "outline",
    shallow = "shallow"
}
export declare enum StudioButtonSize {
    none = "",
    xs = "xs",
    s = "s",
    m = "m",
    lg = "lg"
}
interface ButtonActionProps {
    editor: Editor;
    state: StudioButtonState;
    setState: (props: StudioButtonState) => void;
}
export interface StudioButtonProps extends Omit<StudioBaseProps<HTMLButtonElement>, 'onClick' | 'label' | 'size' | 'type' | 'content'> {
    label?: ReactNode | ((props: {
        editor?: Editor;
    }) => ReactNode);
    variant?: `${StudioButtonVariant}`;
    tooltip?: string;
    buttonType?: HTMLButtonElement['type'];
    active?: boolean;
    icon?: ButtonWithTooltipProps['icon'];
    size?: StudioButtonSize | `${StudioButtonSize}`;
    editorEvents?: Record<string, (props: ButtonActionProps & {
        fromEvent: Record<string, any>;
    }) => void>;
    onClick?: (props: ButtonActionProps & {
        event: MouseEvent<HTMLButtonElement>;
    }) => void;
}
export interface StudioButtonState extends Partial<Pick<StudioButtonProps, 'variant' | 'className' | 'tooltip' | 'label' | 'icon' | 'active' | 'size' | 'disabled'>> {
}
export declare const StudioButton: import("react").NamedExoticComponent<StudioButtonProps>;
export default StudioButton;
