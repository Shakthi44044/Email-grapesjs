import { ElementType } from 'react';
import { ButtonProps } from '../Button';
import { PosOptions } from '../Tooltip';
export type RenderProps = {
    close(): void;
};
export interface PopoverProps {
    handler?: React.ReactNode;
    children?: React.ReactNode | ((props: RenderProps) => void);
    title?: React.ReactNode;
    className?: string;
    static?: boolean;
    clickAway?: boolean;
    fixed?: boolean;
    padding?: boolean;
    overlay?: boolean;
    pos?: PosOptions;
    canOpen?: () => boolean;
    open?: boolean;
    onClose?: () => void;
    size?: 's' | 'sm' | 'md';
    topIcon?: boolean;
    buttonAs?: ElementType<any>;
    buttonProps?: ButtonProps;
}
export default function Popover({ children, handler, title, size, padding, overlay, clickAway, topIcon, pos, open: isOpen, onClose, buttonAs, className, buttonProps }: PopoverProps): import("react/jsx-runtime").JSX.Element;
