/// <reference types="react" />
import { GridProps } from '../Grid';
export interface Props extends GridProps {
    property?: any;
    label?: string;
    empty?: boolean;
    hasValue?: boolean;
    shallow?: boolean;
    row?: boolean;
    tooltip?: React.ReactNode;
    onClear?: () => void;
}
export default function PropertyLabel({ label, property, hasValue, empty, onClear, children, shallow, row, tooltip, className, ...rest }: Props): import("react/jsx-runtime").JSX.Element;
