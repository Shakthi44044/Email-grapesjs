import { GridProps } from '../Grid';
export interface Props extends GridProps {
    separator?: boolean;
    separatorTop?: boolean;
    bottom?: string;
}
export default function SectorRow({ className, children, bottom, separator, separatorTop, ...rest }: Props): import("react/jsx-runtime").JSX.Element;
