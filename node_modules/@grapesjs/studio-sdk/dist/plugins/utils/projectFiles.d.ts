import type { Editor, Page } from 'grapesjs';
import { ProjectFile } from '../../types';
interface GetProjectFileOptions {
    page?: Page;
    assetsFolder?: string;
    filenameCss?: string;
    skipProject?: boolean;
    styles?: 'inline' | 'skip';
}
export declare const getProjectFiles: (editor: Editor, opts?: GetProjectFileOptions) => Promise<ProjectFile[]>;
export {};
